# wb-db

## 🧠 Описание проекта

Скрипт `wb2.py` автоматически генерирует случайные артикулы Wildberries, делает запросы к API WB, получает информацию о товаре и сохраняет результат в локальную базу данных `catolog.db`.

Сохраняются:
- название товара  
- характеристики  
- артикул  
- случайно сгенерированная цена  

Проект можно использовать для:
- тестирования работы с базами данных  
- учебных целей  
- небольшого парсинга Wildberries  

---

## 📦 Возможности

- Получение информации о товаре через API Wildberries  
- Сохранение данных в SQLite  
- Автоматическое создание таблицы и индексов  
- Логирование сохранённых товаров  
- Вывод выборок:
  - всех товаров  
  - товаров без названия  
  - товаров дешевле 10 000  

---

## 🗂 Структура проекта

```

wb-db/
│
├── wb2.py             # основной скрипт
├── catolog.db         # база данных (создаётся автоматически)
├── requirements.txt   # зависимости
└── README.md          # документация

```

---

## ⚙️ Установка и запуск

### 1. Клонировать репозиторий
```

git clone [https://github.com/AKSLU/wb-db.git](https://github.com/AKSLU/wb-db.git)
cd wb-db

```

### 2. Установить зависимости
```

pip install -r requirements.txt

```

### 3. Запустить скрипт
```

python wb2.py

````

Скрипт начнёт:
- генерировать случайные артикулы  
- запрашивать карточки товаров  
- сохранять их в базу `catolog.db`  

---

## 📁 Структура базы данных

SQLite таблица создаётся автоматически:

```sql
CREATE TABLE IF NOT EXISTS products(
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT,
    haracteristici TEXT,
    art TEXT,
    price INT
);
````

Также создаются индексы:

```sql
CREATE INDEX idx_art ON products(art);
CREATE INDEX idx_price ON products(price);
```

---

## 🔧 Параметры

### 📌 Прокси (опционально)

```python
proxies = {
    "http": " ",
    "https": " "
}
```

Если нужно — впиши свои прокси.

### 📌 Базовый артикул

```python
base_art = 5007120
```

Скрипт добавляет случайное число и получает артикулы примерно по соседству.

---

## 🧪 Что делает скрипт (пошагово)

1. Создаёт базу и таблицу (если нет)
2. Генерирует 10 случайных артикулов
3. Делает запрос:

   ```
   https://basket-01.wbbasket.ru/.../card.json
   ```
4. Получает:

   * название товара
   * grouped_options
5. Присваивает случайную цену
6. Сохраняет информацию в SQLite
7. Выводит:

   * все товары
   * товары без имени
   * товары дешевле 10 000

---

## 📦 requirements.txt

```txt
requests
```

(модуль `sqlite3` встроен в Python)

---


